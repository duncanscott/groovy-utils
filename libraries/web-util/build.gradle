dependencies {
    implementation group: 'org.apache.groovy', name: 'groovy', version: groovyVersion
}

jar {
    manifest {
        attributes 'Implementation-Title': 'Web Util', 'Implementation-Version': project.version
    }
}
